[project]
name = "python-class"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "pranavshaurya174", email = "pranav@shauryasoft.com" }
]
requires-python = ">=3.14"
dependencies = [
    "bcrypt>=5.0.0",
    "fastapi>=0.141.1",
    "prisma>=0.15.0",
    "uvicorn[standard]>=0.52.4",
]


[project.scripts]
python-class = "python_class:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
